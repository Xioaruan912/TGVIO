{
  "name": "tgvio-player-web",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "test": "tsc --target ES2022 --module ESNext --moduleResolution Bundler --skipLibCheck src/long-video-list.ts --outDir .test-dist && node --test tests/*.test.mjs",
    "preview": "vite preview"
  },
  "devDependencies": {
    "typescript": "^5.7.3",
    "vite": "^6.1.0"
  }
}
