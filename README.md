# TGVIO 视频转发机器人

把视频、图片或链接发给 Telegram 机器人，它会自动帮你**发布到自己的频道**，还可以顺手备份到网盘（WebDAV）。全程自动，手机就能操作。

- 支持直接转发 Telegram 里的视频/图片，也支持发抖音、B站、YouTube 等**链接**。
- 支持一次发很多个，自动合并成一组发布。
- 发布前可以先**预览、排序、选封面、改文字**。
- 发布成功后有结果卡片，可以收藏、分享、撤销。
- 可选把原文件备份到 WebDAV。

---

## 一、开始前要准备什么

你只需要准备 5 样东西：

| 需要什么 | 去哪里拿 |
|---|---|
| 一台能装 Docker 的服务器（VPS） | 任意云服务商，Linux 系统即可 |
| 机器人的令牌（BOT_TOKEN） | 在 Telegram 里找 **@BotFather**，发送 `/newbot` 按提示创建，复制它给的令牌 |
| API_ID 和 API_HASH | 打开 **my.telegram.org**，登录后进入 `API development tools`，创建一个应用即可看到 |
| 目标频道 | 你自己的频道，例如 `@my_channel`（机器人必须是该频道管理员） |
| 你自己的数字用户 ID | 在 Telegram 里找 **@userinfobot**，它会把你的数字 ID 发给你 |

> 小提示：机器人要先加进你的频道并设为**管理员**，否则无法发布。

---

## 二、三步开始

### 第 1 步：把代码放到服务器上

```bash
git clone https://github.com/Xioaruan912/TGVIO.git
cd TGVIO
```

### 第 2 步：运行一键脚本

```bash
bash install.sh
```

在菜单里选择 **`1) 安装并启动`**，然后按提示依次输入前面准备好的 5 个信息即可。脚本会自动生成配置文件、构建并启动机器人。

### 第 3 步：在手机上打开机器人

在 Telegram 里找到你的机器人，发送一次 **`/start`**，就会看到常驻的手机键盘，之后按按钮操作即可。

---

## 三、脚本菜单说明

运行 `bash install.sh` 后可以看到：

| 选项 | 作用 |
|---|---|
| `1) 安装并启动` | 第一次使用选这个；会引导你填必要信息并启动 |
| `2) 查看日志` | 实时查看运行情况，排查问题时用 |
| `3) 停止` | 停止机器人（数据保留） |
| `4) 删除` | 删除机器人和镜像；默认**保留数据**，会二次确认 |
| `5) 重建 / 更新` | 更新代码后重新构建并启动 |
| `6) 查看状态` | 查看机器人是否在运行 |
| `7) 修改配置` | 用菜单方式修改配置（见下一节） |

---

## 四、怎么使用（手机操作）

1. **直接转发**：把视频/图片转发给机器人，它会自动发布。
2. **合集模式**：点键盘上的「📥 新建合集」，把要一起发布的视频/图片陆续发给它，发完后点「👀 预览与整理」，可以排序、选封面、改文字；最后点「✅ 确认发布」。
3. **发链接**：把抖音/B站/YouTube 等链接直接发给机器人即可。
4. **结果与收藏**：发布完成后会出现结果卡片，可以「打开帖子」「收藏」「分享」「撤销」。
5. **我的草稿 / 发布风格 / 发布历史**：都在手机键盘上，点一下就能进。
6. **内容与下载**：在「⚙️ 设置 → 🧩 内容与下载」里可以
   - 🖼 设置**自定义缩略图**（回复任意图片发送 `/thumb`），让所有视频用同一张封面图；
   - 📝 设置**配文模板**（发送 `/caption 模板`，支持 `{channel} {date} {index} {total} {name}` 等变量，还可加一行 `button: 文字 | 链接` 生成按钮）；
   - 🎚 选择**链接画质**（最佳/1080p/720p/480p）或**仅音频（MP3）**。

7. **从"不能转发"的来源推送内容**（可选，全程在手机 Bot 里操作）：
   - 场景：某些资源机器人/频道禁止转发或下载，普通转发给机器人没用。
   - 打开 **`/source`**（或「⚙️ 设置 → 🔐 来源登录」）→ 点「📱 登录 / 重新登录」，按提示发送**手机号 → 验证码 →（两步密码）**。
   - 登录后点「➕ 添加来源」，发送 `@频道名` 或 `-100…` 数字 ID（该账号需已加入）。
   - 抓取方式（推荐）：发送 **`/pick`**，机器人只列出该来源**最近 2 个自然日**内的媒体（默认只看**今天**），每行只给数量与大小，例如 `3) 相册 10 项 · 🎬7 🖼3 · 175.1MB`：
     - 页面顶部一行按钮可在**多个来源之间切换**（当前来源带 ✅）；
      - 打开页面后会**自动生成一张整页缩略图**（**每格左上角标着 1–N 序号**，与列表编号一一对应，不用自己数），生成期间有 `⏳ 正在生成… 3/10` 进度提示；缩略图按来源消息 id **缓存 48 小时**，翻页/返回不会重复下载；
      - 图片若没有内嵌缩略图会自动取原图（≤2MB），视频若没有缩略图只拉前 4MB 抽一帧（≤6 秒），仍然失败才显示"无预览"；
      - 每行三个按钮：**📥 = 抓取**（先弹确认卡，✅ 才真正入队）、**👁 = 整组预览**（把这一组的全部图片+视频拼成一张带序号的缩略图）、**☑️ = 加入合并**；
      - **👁 预览会累积**（最多同时留 10 张、10 分钟自动消失），可以连着看好几组对比"到底哪几个是一组的"；每张预览自带 `[☑️ 选择 N] [📥 抓取] / [🗑 移除] [⬅️ 返回列表]`，**可以直接在缩略图上勾选**，不用回列表；勾选/翻页/换来源/刷新都**不会删**预览，只有 ✅ 发布成功、📥 抓取成功、点 `🧹 清理预览 (N)`、超过 10 张或 10 分钟到期才清。
      - **已提交的不再出现**：抓过/发过的条目会从列表里隐藏（页头显示 `已提交 N 条（不再抓取）`），点 `✅ 已提交 (N)` 可以看只读清单；**连"同一条内容被来源重新上传"（新消息号、同大小/分辨率/配文）也会一起隐藏**。失败或取消的任务会自动回到列表；想重发某条内容，在已提交页点 `♻️ 允许重新抓取 N` 放行即可。再次点这些条目的抓取/勾选会被拒绝。
      - **排序可微调**：合并确认卡按选择顺序列出每一组，每组一个 `🗑 移除 N`，删掉不需要的那组再发布。

     - **合并发布**：把几行勾成 `✅` 后，底部出现「✅ 发布已选 (N)」→ 确认卡显示 `共 N 组 · M 项 · 🎬x 🖼y · 大小` → ✅ 后把选中的组**合成一个相册**发布（图片和视频也会放在同一个相册里；每 10 项一个相册，发在同一个频道帖的评论区）；以前发过的条目会自动跳过并提示数量；一次上限默认 100 项，可用 `TGVIO_MERGE_MAX_ITEMS` 调整。
     - `今天` / `近2天` 与 `只看视频` 可组合筛选；
     - 列表**不显示配文**（来源的配文基本都是广告）。
     - **疑似广告自动隐藏**：来源机器人爱往列表里塞广告（孤立图片、反复重发的同一条内容）。`/pick` 会零下载地在同一趟扫描里给每条打分，达到阈值就**默认隐藏**，页头显示 `已隐藏 N 个疑似广告`：
       - 判定只看结构信号，**不看文案关键词**——来源会给真实内容也加推广页脚（同一条文案挂在 22 个不同文件上），这种文案一律当页脚忽略；
       - 相册（含合并发布）永远不隐藏；孤立的真实大视频也不会因为"孤立"被隐藏；
       - 想全部看：点 `🚫 广告过滤 ✅` 关掉过滤；点 `👀 查看被隐藏 (N)` 打开隐藏页——自动生成整页缩略图（位置=行号），每行可 `✅ 放行`（这条内容以后**不再隐藏**，按内容指纹持久化）或 `📥 抓取`；
       - 列表扫描范围=最近 2 天窗口内最新的 400 条；扫描结果缓存 5 分钟，点 `🔄 刷新` 立即重扫。
   - 抓取方式二：把频道的消息链接（`https://t.me/…`）发给机器人。
   - 发布出去的内容会自动带**分组标头**：`🗂 09-19 #15 · 10 个视频 · @来源`，频道帖和评论区每个相册的首条都有，一眼能认出哪几条是同一组（相册超过 10 条时标 `分卷 1/2`）。
   - 只在你添加的来源里生效，**不会自动监听**任何聊天。
   - ⚠️ 该登录使用你自己的账号，session 等于账号权限；请只处理你拥有或已获授权的内容，并自行承担账号风险。

---

## 五、修改配置

在脚本菜单选 **`7) 修改配置`**，会看到分组菜单：

```
1) 必填凭证     机器人的令牌、API、目标频道、你的用户 ID
2) 发布外观     是否生成封面、是否保留原来的文字等
3) 功能开关     各种功能开/关
4) 归档(备份)   把文件备份到 WebDAV 网盘
5) 性能与高级   并发、缓存、日志等（一般不用改）
6) 查看全部     查看当前配置（密码会打码）
7) 备份 / 恢复   修改前自动备份，可随时恢复
```

- 选择分组 → 再选择要改的某一项 → 输入新值（**直接回车 = 不修改**）→ 保存。
- 每项都有中文说明，填错会提示重填，不会把错误值写进去。
- 修改保存后，脚本会问你要不要**立即重启**让改动生效。

---

## 六、常见问题

**发了消息没反应？**
- 确认机器人正在运行（脚本菜单 `6) 查看状态`），并确认你的用户 ID 已填对。

**发布失败 / 上传不上去？**
- 先看日志（菜单 `2) 查看日志`）。多半是网络问题，稍等会自动重试。
- 如果是频道权限问题：确认机器人已是频道**管理员**。

**磁盘满了？**
- 机器人会定期清理已完成的临时文件。也可以手动停止后删除 `downloads/` 里的内容。

**怎么彻底卸载？**
- 脚本菜单选 `4) 删除`；如果想连数据一起删，再按提示选择删除数据即可。

---

## 七、数据与备份

运行数据都在项目目录下，**不会上传到任何地方**：

```
data/        数据库（任务与状态）
session/     Telegram 登录信息（请勿外传）
downloads/   下载缓存
logs/        运行日志
.env         你的配置（含密钥，请勿外传）
```

> 换服务器时，把这几个目录一起拷过去即可（`session/` 能让机器人免重新登录）。

---

## 八、想让 AI 帮你二次开发？

本仓库包含一份专门写给 AI 的开发说明：[`AI_DEVELOPMENT.md`](AI_DEVELOPMENT.md)。它介绍了项目结构、分层规则、核心数据流和关键接口，AI 读完就能快速上手。

用法：把本仓库地址发给你使用的 AI，并附上下面这句话：

```text
请先完整阅读仓库根目录的 AI_DEVELOPMENT.md，了解项目结构、分层规则、核心数据流和关键接口，然后再帮我实现下面的需求：
（在这里写你的需求）
```

---

## 九、开源许可

见 [LICENSE](LICENSE)。
