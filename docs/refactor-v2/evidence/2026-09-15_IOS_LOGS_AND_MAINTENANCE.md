# 2026-09-15 iOS 使用记录核查与清理时间调整

## 使用记录（北京时间）

- 用户说明从 iOS 发送；服务端日志本身不证明客户端系统类型。
- 2026-09-14 23:36:31～23:39:32 创建 3 个任务，共 18 个视频、3 张图片。
- 最后一个 Telegram 发布于 23:50:28 完成；3 个 Job 均 succeeded，3 个 Archive package 均 committed，无任务 error_code。
- 发布步骤：3 个频道封面步骤、3 个讨论区视频组步骤均 succeeded；记录 5 个频道消息和 18 个讨论区消息凭据。
- 三次 `publish.discussion_root.not_observed` 是频道发送后线程根捕获警告；后续讨论区步骤成功，不能据此判定评论区发布失败或 iOS 故障。
- 无效果预览请求、无合集草稿记录；本次不能作为草稿编辑或效果预览实机验收。
- 当前日志保留范围内另有 9 月 14 日校时触发的 singleton lease lost，与此次发送时间不同。

## 运维配置调整

- 生产 runtime flag `daily_cleanup_time` 持久设置为 `19:00`（Asia/Shanghai）。
- 写入前在 SQLite 写事务中确认无非终态 Job、无未提交 Archive；仅更新该配置键，未删除历史或媒体。
- 单个现有 tgvio 容器重启以加载 RuntimeFlags；未构建镜像、未修改源码、未创建第二个 Bot。
- 9 月 15 日 06:00 的 daily_hide 已完成，保持每日一次语义，不重置 maintenance ledger。因此下次新维护为 9 月 16 日 19:00 左右（受轮询间隔影响）。
- 不修改每日任务编号/筛选仍采用的 06:00 业务日边界。
- 已知显示限制：设置页说明将 06:00 写死，可能仍显示旧时间；实际调度读取持久 runtime flag。该 UI 文案需后续代码发布改为动态读取，不能把本次配置调整说成 UI 已同步修改。
- 时间同步已开启；AGENTS.md 中 ntp=no 等旧描述不代表本次实时状态。

本次只诊断用户使用记录，不执行重发、撤销、远端删除或媒体实机测试。
